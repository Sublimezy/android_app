<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- view背景色 -->
    <solid android:color="@color/all_line_color" />
    <!-- 边框圆角 -->
    <corners
        android:bottomRightRadius="@dimen/si"
        android:bottomLeftRadius="@dimen/si"
        android:topLeftRadius="@dimen/si"
        android:topRightRadius="@dimen/si" />
    <size
        android:width="@dimen/bashiwu"
        android:height="@dimen/sanshi"
        />

</shape>